---
---

Add a `dev` branch CDN test channel: pushing to `dev` builds and deploys to a Cloudflare Pages preview, reachable at `js.maytes.co/dev/checkout-button.js` for internal pre-merge testing. This replaces the old `/dev/` alias, which tracked whatever `main` last released.
