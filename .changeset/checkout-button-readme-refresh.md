---
---

Docs only — no release. Refresh the checkout-button READMEs and technical overview: correct the launch-mode description (same-window redirect by default, `mode: 'popup'` opt-in), sync the public README's usage snippet and API table with the internal one, remove the internal `/dev/` rolling channel from the public README, and add a mobile-app deep-link note (shared payment links open the Maytes app on installed devices).
