---
---

Drop documentation left over from the private monorepo: run commands from the repository root, link the public integration guide instead of the private INTEGRATION.md, and mark the release pipeline as live (docs only, no runtime package change).
