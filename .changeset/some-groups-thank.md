---
---

Sync main with the CDN Cache Rules fixes and docs cleanup that landed after PR #5 merged: exclude type declaration files from the immutable-pins rule, derive CDN_HOST from a single source of truth, and remove the internal implementation-spec doc from docs/ (tooling/docs only, no runtime package change).
